[Оглавление](../../index.md) · [Обзор архитектуры](../overview.md) · [English version](../../../en/20-architecture/components/storage.md)

# Слой хранения

Интерфейс между логикой шлюза и базой данных. Компоненты выше него SQL не видят вообще.

## Уровень интерфейса

Абстракция проведена на уровне операций предметной области — «сохрани поступление», «дай
адреса кошелька», «отметь адрес занятым» — а не на уровне «универсального SQL». Конкретная
реализация выполняет эти операции своим диалектом.
См. [ADR-0006](../decisions/0006-storage-abstraction.md). Это тот же принцип, что у
[абстракции блокчейна](chain-abstraction.md): компоненты выше интерфейса не знают, что под
ним.

Интерфейс также несёт **операции чтения под экран** — по одному широкому, осмысленно
названному чтению на экран или читающий путь API (сводка дашборда, страница истории) —
вместо универсального конструктора запросов (дополнение ADR-0006). Слой организован по
предметным областям: операции реестра; операции пользовательской базы для финансового
пути (транзакции, балансы, привязки), кошельков, приложений и экранных чтений.
Семантика финансовой модели — допустимые направления и статусы, классификация
транзакции для потребителя — живёт в одной точке внутри слоя и является единственным
источником правды для всех потребителей.

## Хранение по пользователям

У каждого пользователя — своя база данных и рабочая директория
(см. [ADR-0005](../decisions/0005-multi-user-model.md)):

- Изоляция структурная: запрос физически не может дотянуться до данных другого пользователя.
- Жизненный цикл пользователя (резервная копия, перенос, удаление) — операция над одним
  каталогом.
- Конкуренция записи в SQLite размазана по файлам пользователей; используется режим журнала
  WAL, чтобы читатели не блокировались писателем.

## Общая реестровая база

Помимо пользовательских баз шлюз ведёт одну небольшую общую базу с реестровой ролью
(см. [ADR-0008](../decisions/0008-shared-registry-db.md)): реестр пользователей с учётными
данными для входа, индекс API-ключей, учётные записи операторов, общие настройки шлюза,
справочник активов и служебное состояние watcher по сетям
(см. [ADR-0010](../decisions/0010-networks-assets-financial-data.md)). Финансовых данных в
ней нет — кошельки, адреса, привязки и операции живут только в пользовательских базах.

## Реализации

- **SQLite** — первая реализация: база пользователя — файл в его директории.
- **PostgreSQL, MySQL** — следующие по плану. Как изоляция «по пользователю» ляжет на
  серверную СУБД (база на пользователя или схема на пользователя) — открытый вопрос,
  который будет решён отдельным ADR при подключении PostgreSQL.

## Индексы

База пользователя индексирует то, чем на самом деле спрашивают горячие чтения: входящие
строки по направлению и порядку блоков (история и кабинета, и API приложений), строки,
ещё не учтённые в балансе, и предварительные строки по номеру блока (чистка после
перестройки цепи). Последние два чтения выполняются каждым проходом по каждой базе в
каждой сети, даже когда делать нечего. Без индексов каждое из них сканировало таблицу
целиком.

## Миграции

Миграции схемы работают на SQLAlchemy Core + Alembic
(см. [ADR-0013](../decisions/0013-backend-stack.md)) двумя потоками — реестр и
пользовательские базы; пользовательский поток обходит все базы, найденные на диске,
и останавливается на первом сбое, называя повреждённый файл. Каждая мигрируемая база
фиксируется в журнале.

## Связанные документы

- [Обзор архитектуры](../overview.md)
- [Оркестратор](orchestrator.md)
- [Watcher](watcher.md)
- [Модель данных](../data-model.md)
- [ADR-0005: Многопользовательская модель](../decisions/0005-multi-user-model.md)
- [ADR-0006: Абстракция хранения](../decisions/0006-storage-abstraction.md)
