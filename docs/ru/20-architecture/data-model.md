[Оглавление](../index.md) · [Обзор архитектуры](overview.md) · [Слой хранения](components/storage.md) · [English version](../../en/20-architecture/data-model.md)

# Модель данных

Концептуальная структура данных шлюза: сущности, их поля и связывающие их правила. Точные
определения таблиц (типы, индексы) закрепляются на этапе реализации. Где какие данные живут —
задано [ADR-0005](decisions/0005-multi-user-model.md) и
[ADR-0008](decisions/0008-shared-registry-db.md); сети, активы и финансовые структуры —
[ADR-0010](decisions/0010-networks-assets-financial-data.md).

## Пользовательская база

### Привязки

Основная таблица соответствия (см.
[ADR-0009](decisions/0009-address-bindings-primary-mode.md)): кошелёк, сеть, адрес, мемо,
приложение, пользователь приложения.

- Одна привязка на «кошелёк + сеть + приложение + пользователь приложения». Кошелёк несёт
  семейство (TRON, EVM, …); привязка называет конкретную сеть.
- В сетях с мемо привязку идентифицирует пара «адрес + мемо»; много привязок могут разделять
  один адрес.
- Ссылка на приложение хранится рядом со ссылкой на пользователя приложения намеренно
  (дешёвая фильтрация по приложению); согласованность двух ссылок обеспечивается на уровне
  схемы.
- Финансовых данных нет.

### Приложения

Единица подключения к шлюзу (см.
[ADR-0009](decisions/0009-address-bindings-primary-mode.md)): принадлежит пользователю
шлюза, идентифицируется своим API-ключом (индекс «хеш ключа → пользователь» живёт в общем
реестре). Несёт соответствие «сеть → кошелёк», настраиваемое владельцем в кабинете, — сами
приложения кошельков не видят (см.
[ADR-0011](decisions/0011-application-api-principles.md)).

### Пользователи приложений

Внутренний id, приложение, внешний идентификатор — произвольная строка, которую присылает
приложение; шлюз её не интерпретирует. Уникален в рамках приложения.

### Балансы

Одна строка на «адрес + актив»: текущий баланс, всего поступлений, дата/время последнего
поступления.

- Производные данные: обновляются в той же транзакции БД, что и вставка подтверждённой
  транзакции, и в любой момент пересчитываемы из таблицы подтверждённых.
- Учитываются только подтверждённые транзакции; ожидающее видно в таблице неподтверждённых.

### Транзакции

Три таблицы с общим ядром полей: адрес, идентификатор транзакции, актив, направление, сумма,
номер блока (блок, в который транзакция попала, — записывается один раз и не меняется),
дата/время.

- **Неподтверждённые** — транзакции «в полёте»; единственная изменяемая таблица, всегда
  маленькая. Дополнительное поле: статус. Количество подтверждений не хранится — оно
  вычисляется как «текущая высота сети − номер блока транзакции».
- **Подтверждённые** — финальный успех; журнал только на добавление, строки никогда не
  обновляются.
- **Неуспешные** — финальный провал; только добавление, хранится для разбирательств, на
  балансы не влияет.

Правила:

- Транзакция покидает «неподтверждённые» атомарно: одна транзакция БД удаляет её из горячей
  таблицы, вставляет в финальную и (для подтверждённых) обновляет строку баланса.
- Идемпотентность: повторное сканирование тех же блоков не должно создавать дублей —
  уникальный ключ «идентификатор транзакции + адрес + актив» (уточняется при реализации,
  если одна транзакция может нести несколько переводов одного актива на один адрес).

## Общая реестровая база

Помимо реестра пользователей, индекса API-ключей, учёток операторов и настроек
(см. [ADR-0008](decisions/0008-shared-registry-db.md)):

### Справочник активов

Сетеспецифичные справочные данные: сеть, нативная монета или контракт токена, символ,
десятичность. USDT в TRON и USDT в Ethereum — два разных актива; семейство (например, EVM)
здесь не встречается — только конкретные сети. Справочник пополняется автоматически, когда
watcher впервые видит новый актив: шлюз не ограничивает список принимаемых активов — честно
записывает всё, что пришло; фильтрация — дело потребителя.

### Служебное состояние watcher

По каждой сети: высота последнего обработанного блока. Одновременно точка возобновления
сканирования после остановки и высота, от которой вычисляется количество подтверждений.
Не финансовые данные.

## Связанные документы

- [Слой хранения](components/storage.md)
- [Watcher](components/watcher.md)
- [ADR-0009: Постоянные привязки адресов как основной режим](decisions/0009-address-bindings-primary-mode.md)
- [ADR-0010: Сети, активы и структуры финансовых данных](decisions/0010-networks-assets-financial-data.md)
